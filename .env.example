# Vercel Postgres / Neon / any Postgres URL.
# On Vercel: Storage → Create Database → Postgres, then pull env vars.
POSTGRES_URL=
DATABASE_URL=

# Set to "http" after the database is linked so the app uses /api/family.
# Leave unset to keep the in-memory + localStorage client.
VITE_FAMILY_API=
